
/**
 * Module defining default GW configurations for webpack build and dev server
 */

const webpackConfig = require('./webpack.config');
const webpackDevServerConfig = require('./webpackDevServer.config');

module.exports = {
    webpackConfig,
    webpackDevServerConfig
};
